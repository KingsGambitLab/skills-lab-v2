# VS Code walk-agent — kimi v1 — 2026-04-27

- Course: `created-698e6399e3ca` (Open-Source AI Coding: Aider + Kimi K2)
- Pass tag: `v1`
- Repo: `/Users/tushar/Desktop/codebases/skills-lab-v2`
- Extension package: `vscode/skillslab-0.1.0.vsix` — **29,767 bytes (29.07 KiB)**
- Pre-built `dist/` present (9 .js + 9 .js.map files matching `src/*.ts` 1:1).

## EXPLICIT INVARIANTS

### I. TypeScript compiles cleanly — PASS

```
$ cd vscode && npx tsc -p .
EXIT=0
```
Zero `error TS` lines.

### II. SecretStorage is ONLY persistence for keys — PASS

```
$ grep -nE 'localStorage|sessionStorage|writeFile.*token|fs\.appendFileSync.*key' vscode/src/*.ts
EXIT=1   (no matches)
```

`auth.ts` uses only `ctx.secrets.store/get/delete` for bearer + ANTHROPIC + GITHUB keys. `state.ts` uses `globalState` (no secrets, only attempt counters / cursor).

### III. CLI token inheritance is ONE-WAY — PASS

```
$ grep -nE 'fs\.writeFile|fs\.appendFile' vscode/src/auth.ts
EXIT=1   (no matches)
```

`tryAdoptCliToken` only `fs.readFileSync(tokenPath, "utf8")` (auth.ts:94). Default policy `ask` → consent prompt at lines 107-113.

### IV. WebView CSP is strict — PASS

```
$ grep -nE "unsafe-inline.*script|script-src 'unsafe" vscode/src/*.ts
EXIT=1   (no matches)
```

`webview.ts:84` builds CSP `script-src 'nonce-${nonce}'`. Per-render nonce generated by `widgets.ts:generateNonce()`. Inline scripts captured + re-injected under nonce only. `'unsafe-inline'` appears ONLY for styles (allowed by spec — Rich-emitted HTML carries inline styles).

### V. Outer-IIFE unwrap handles both shapes — PASS

`widgets.ts:103-107`:
```ts
function stripOuterIife(code: string): string {
  const re = /^[;\s]*\(\s*function\s*\(\s*\)\s*\{([\s\S]*)\}\s*(?:\)\s*\(\s*\)|\(\s*\)\s*\))\s*;?\s*$/;
  const m = code.trim().match(re);
  return m ? m[1] : code;
}
```

Coverage analysis:
- Crockford `(function(){...})()` → matches `\)\s*\(\s*\)` branch. PASS
- Alt `(function(){...}())` → matches `\(\s*\)\s*\)` branch. PASS
- Leading-semicolon `;(function(){...})()` → leading `^[;\s]*` covers. PASS
- IIFE-with-args `(function(x){...})("y")` → opening `\(\s*\)` requires empty parens; does NOT match. PASS (correctly leaves args-IIFEs untouched)

### VI. Devcontainer files on all 3 course-repos — PASS

```
=== kimi-eng-course-repo ===     6 ok, 5 branch_404
  module-1-starter: ok    module-2-claudemd: ok    module-3-agents: ok
  module-4-hooks:   ok    module-5-mcp:      ok    module-6-capstone: ok
=== aie-course-repo ===           6 ok, 5 branch_404
  module-1-starter: ok    module-2-retry:    ok    module-3-iterate:  ok
  module-4-mcp:     ok    module-5-team:     ok    module-6-agent-harness: ok
=== jspring-course-repo ===       6 ok, 5 branch_404
  module-1-starter: ok    module-2-claudemd: ok    module-3-agents: ok
  module-4-hooks:   ok    module-5-mcp:      ok    module-6-capstone: ok
```

All 18 working branches across 3 repos have `.devcontainer/devcontainer.json`. Missing branches are `branch_404` (don't exist on that course — spec allows: "Branches that don't exist on a particular course-repo are fine"), zero `file_404` (which would mean branch exists but no devcontainer).

### VII. URI handler routes correctly — PASS

`extension.ts:96-121`:
```ts
vscode.window.registerUriHandler({
  handleUri: async (uri) => {
    const m = uri.path.match(/^\/course\/([^/]+)$/);
    ...
    const stepId = params.get("step");
    ... await cmds.openStep(courseId, mod.id, target);
```

Regex matches `/course/<courseId>`, reads `step` from query, walks modules to find the target step ID, calls `cmds.openStep(...)`. Conforms to `vscode://tusharbisht1391.skillslab/course/<id>?step=<sid>`.

### VIII. Buddy-Opus MVP cuts honored — PASS

```
$ ls vscode/src/*.ts | wc -l
9                                       # (8 + theme — matches spec)

$ grep -nE 'createStatusBarItem' vscode/src/*.ts
EXIT=1   (no matches)                   # status bar correctly cut

$ grep -cnE 'submitAndContinue' vscode/src/*.ts
extension.ts:1   commands.ts:3   (≥3 — total 4 across files)
```

Files: `api.ts auth.ts commands.ts extension.ts state.ts theme.ts tree.ts webview.ts widgets.ts` — 9 files matching the buddy-Opus split.

### IX. LMS frontend has additive 'Open in VS Code' button — PASS

`frontend/index.html:4671`:
```html
<a href="vscode://tusharbisht1391.skillslab/course/${esc(state.currentCourse && state.currentCourse.id || '')}?step=${esc(String(rawStep.id || ''))}" ...>
```

Publisher.id matches the extension manifest (`tusharbisht1391.skillslab`). VS Code will intercept.

### X. 'How to run' affordance present — PASS

`webview.ts:7,93,99` — banner comment "How to run this step section that opens the integrated terminal", `runInTerminal` message handler at line 48, prominent button at line 99: `<button class="primary" data-vsc-msg="runInTerminal">▸ Open Terminal &amp; Run Steps</button>`. Web-surface fallback shows "Open in Browser" instead.

## BEHAVIORAL CHECKS

### B1. Server HTML rewrite test — PASS (static analysis)

Fetched real step content from prod LMS:
```
$ curl -s http://52.88.255.208/api/courses/created-e54e7d6f51cf/modules/23201
STATUS=ok  STEP=85111  CONTENT_LEN=9599
```

Step content begins with `<div style="background: #1e2538; color: #e8ecf4; padding: 20px; ...">` — confirms server emits inline-style HTML that the rewrite pipeline must handle. `widgets.ts:rewriteForWebview()`:
1. `extractAndStripScripts` regex `/<script\b[^>]*>([\s\S]*?)<\/script\s*>/gi` — strips script tags + captures bodies (lines 42-55).
2. `rewriteOnclicks` regex on lines 67-84 converts `onclick="fn()"` → `data-action="fn" data-args-raw=""`.
3. `buildRuntimeScript` (lines 115-163) wraps captured scripts inside a per-script IIFE that runs `stripOuterIife(raw)` then computes hoist-tail `try{if(typeof X!=="undefined")window["X"]=X;}` for every named function/const/let/var. After IIFE-unwrap, hoisted names are at outer scope so the hoist-tail picks them up. Delegator at lines 144-160 looks up `window[name]`, evaluates `data-args-raw` via `Function(...)`, invokes. Mirrors frontend v7 fix.

DEFERRED-TO-MANUAL: live VS Code render of step 85111 to visually confirm a `generateComparison` button works. Static pipeline analysis matches the canonical browser implementation (per CLAUDE.md "data-action delegator").

### B2. Auth flow simulation — PASS

`auth.ts`:
- Line 87: `if (policy === "never") return null;` — early-out
- Line 90: `if (!tokenPath || !fs.existsSync(tokenPath)) return null;`
- Line 100-104: `policy === "always"` → silent adopt
- Line 107-113: `policy === "ask"` → `vscode.window.showInformationMessage(...Adopt..."Don't ask again", "No")`
- Line 119-125: `Don't ask again` updates config to `never` (sticky)

Correct flow for all 4 cases the spec enumerates.

### B3. Submit & Continue context-awareness — PASS

`commands.ts`:
```
191:   * - POST /api/exercises/validate with attempt_number
197:    const attempt = this.state.recordAttempt(slug, step.id);
215:        await this.api.markComplete(step.id, 100);    (correct path)
251:        await this.api.markComplete(step.id, Math.round(score * 100));
```

`recordAttempt → api.validate(attempt_number) → markComplete on correct → advance cursor / open next`. All three required calls present.

## RECENTLY-FILED ISSUES

1. Extension TS compiles cleanly — **CLOSED** (Inv I, EXIT=0).
2. SecretStorage only key persistence — **CLOSED** (Inv II, zero matches).
3. CLI token inheritance one-way — **CLOSED** (Inv III, no fs.write/append in auth.ts).
4. WebView CSP nonce-only for scripts — **CLOSED** (Inv IV, `script-src 'nonce-${nonce}'` at webview.ts:84).
5. Outer-IIFE unwrap handles `})()` and `}())` — **CLOSED** (Inv V, regex covers both shapes + leading-semicolon, args-rejection by construction).
6. Devcontainer pushed to all 3 course-repos on every module branch — **CLOSED** (Inv VI, 18/18 working branches green).
7. URI handler routes `vscode://...skillslab/course/<id>?step=<sid>` to openStep — **CLOSED** (Inv VII, extension.ts:96-121).
8. MVP cuts honored (no status bar; submit+check merged; 9 files) — **CLOSED** (Inv VIII).
9. LMS frontend has additive `vscode://tusharbisht1391.skillslab` button — **CLOSED** (Inv IX, frontend/index.html:4671).
10. WebView "How to run" affordance — **CLOSED** (Inv X, runInTerminal button at webview.ts:99).

## SUMMARY

- Invariants passed: **10 / 10**
- Behavioral checks passed: **3 / 3** (B1 with DEFERRED-TO-MANUAL on a live VS Code render)
- VSIX size: **29,767 bytes (29.07 KiB)** at `/Users/tushar/Desktop/codebases/skills-lab-v2/vscode/skillslab-0.1.0.vsix`
- Top issues: none

### Verdict: SHIP
