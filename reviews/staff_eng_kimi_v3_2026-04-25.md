# Staff-Engineer v3 Re-review — Kimi K2 + Aider Course (post round-5 regen)

- Reviewer: senior staff engineer hat. 10+ yr Python/FastAPI/MLOps; expert OpenAI-compat APIs; shipped MCP integrations; Aider in 3 production teams.
- Date: 2026-04-25 (round-5 regen sweep)
- Course: `created-698e6399e3ca` — http://localhost:8001/#created-698e6399e3ca
- Method: pulled raw step JSON via `GET /api/admin/courses/{id}/raw`; cross-referenced live repo (`tusharbisht/kimi-eng-course-repo` module-6-capstone branch) for GHA workflow; verified Aider command surface against https://aider.chat/docs/usage/commands.html.
- Regen sweep audited: **85138** (M0.S1 IS/ISN'T), **85152** (M3.S3 audit-endpoint), **85159** (M5.S2 MCP adapter), **85165** (M7.S4 AI code review).

---

## TL;DR

**Verdict: SHIP-WITH-FIXES.** Morning's two P0s are PARTIALLY closed. v2's verdict (SHIP-AS-CANONICAL) overstates: v2 quoted text that is **not present in the live step JSON for 85165**, and described an Aider command (`/load`) that the regen no longer uses (regen uses `--message-file` and `/read-only` instead — both real but different). Net: P0-A2 STILL OPEN in softer form; P0-A1 CLOSED. Two new **P1 regressions** in non-regenerated content (85164 capstone tells learners to use `deepseek-chat`; 85155 harness uses Moonshot-direct endpoint while rest uses OpenRouter). One copy-drift on coverage gate (narrative says 85%, actual GHA is 80%).

I'd sign off shipping this for a 50-engineer team **after ~20 min of narrow per-step edits**, not as-it-stands.

---

## P0 closure table

From `reviews/staff_eng_kimi_aie_postfix_2026-04-25.md`:

- **P0-A1**: M5.S2 (85159) cloned the AIE MCP repo (`tusharbisht/aie-team-tickets-mcp`) without explanation — looked like cross-course leak.
- **P0-A2**: M7.S4 (85165) used `pydantic_idempotency` (fictional package) as planted bug without telling learner it was planted.

| P0 | Today's regen state | Verdict |
|---|---|---|
| **P0-A1** (cross-course MCP repo gloss) | Top-of-step "MCP Without the Magic" panel verbatim: *"This course reuses the MCP server from our Claude Code course (`tusharbisht/aie-team-tickets-mcp`). MCP is a protocol, not an Anthropic-only thing — any LLM that can dispatch OpenAI-compatible tool_calls can consume it. We'll use a tiny Python adapter (50 lines) to bridge the MCP's stdio JSON-RPC into Moonshot's tool_call format."* Reframes sloppiness as pedagogy. | **CLOSED** |
| **P0-A2** (planted-hallucination flag) | Briefing has generic "Bug Categories to Hunt For" listing "Dependencies & Libraries: Non-existent packages" as first item. **Does NOT name `pydantic_idempotency` in the briefing**. Does NOT have v2's claimed "IMPORTANT" warning paragraph. | **STILL OPEN, soft form** (∼60% closed) |

### P0-A2 evidence — what's actually in the regen

v2 (lines 39-44) quoted as load-bearing evidence:
> **IMPORTANT:** This 60-line diff was generated by Kimi K2 in a real session — including its mistakes. ONE of the flaws is a HALLUCINATED LIBRARY (Kimi pinned `pydantic_idempotency==1.x.x` which does NOT exist on PyPI)...

I just pulled the raw 85165 JSON and grep'd: **no occurrence of "IMPORTANT", no occurrence of "planted", no occurrence of `pydantic_idempotency`** in the rendered concept content. The string appears only in `demo_data.code` (where it's planted) and in `demo_data.bugs[0].description` (post-submit feedback metadata). The briefing's category list is generic — a senior eng who already burned 30 min pip-installing `pydantic_idempotency` IRL still doesn't see "this exact package is fictional on purpose; don't go look for it."

**Fix to fully close**: ONE sentence in the briefing — *"Note: one planted bug is a hallucinated import (`pydantic_idempotency`) — fictional on purpose, do not pip-install. It demonstrates a real Kimi K2 failure class."*

---

## Per-step technical depth audit

### 85138 (M0.S1) — IS/ISN'T framing — **B+**

- Hook: *"Your team needs AI coding without vendor lock-in. Can open-source tools compete with Claude Code?"* Clean, intentional Claude Code reference as the proprietary baseline.
- "What This Course IS" cards: Terminal-first, BYO-key, Production features (POST /orders + GHA), Open-source stack. ✓
- "ISN'T" list: 4 negatives including "Claude Code tutorial (different tool)". Clean.
- Live "Aider Workflow Simulator" with `/add`, `/test`, `/commit` button-mocks — all real Aider commands.
- **Missing vs v2 claim**: v2 (lines 65-67) said *"What This Course IS list ends with **Zero Anthropic dependency** — fully open-source stack."* — verified false; actual end-card says *"Open-source stack — Aider + Kimi K2, no vendor lock-in"*. Functionally similar; v2's verbatim quote is fabricated. Tighten card text to nail the morning marketing promise.

### 85152 (M3.S3) — `/audit-endpoint` custom command — **A−**

The morning beginner reviewer flagged the prior version's `.aider/commands/audit-endpoint.md` + `{{ARG1}}` template + `/audit-endpoint orders` invocation as a fabricated Aider feature. Confirmed: regen no longer uses any of those.

Regen teaches `prompts/audit-endpoint.md` loaded via:
- `aider --message-file prompts/audit-endpoint.md src/routes/orders.py` — **REAL** (Aider's documented one-shot send-message-from-file flag).
- `/read-only prompts/audit-endpoint.md` — **REAL** Aider 0.40+ command ("Add files to the chat that are for reference only, not to be edited").

**Note**: v2 (lines 100-110) claimed regen uses `/load`. **It does not.** Regen uses `--message-file` + `/read-only`. Both real, just different from v2's claim. All 4 user-prompt audit categories are covered:
- **Auth**: "Proper auth decorator/middleware applied", "User context validation", RBAC ✓
- **Exception handlers**: "Comprehensive exception handling", proper status codes, no sensitive data in errors ✓
- **Response schema**: Pydantic validation, consistent response schema ✓
- **N+1**: "No N+1 query patterns" ✓

**Pedantic note**: checklist lists `select_related/prefetch_related` (Django ORM) under "Database Performance". This is a SQLAlchemy course — equivalents are `selectinload/joinedload`. A senior eng will downgrade trust slightly. P2.

### 85159 (M5.S2) — Python adapter for stdio JSON-RPC — **A−**

- P0-A1 gloss panel closes the cross-course leak.
- Adapter scaffold (`__init__`, `start_server`, `send_request(method, params)`, `get_tools`, `close`) matches MCP stdio protocol.
- `tools/list` invocation `echo '{"jsonrpc":"2.0","method":"tools/list","params":{},"id":1}' | node index.js` — **real MCP wire shape** per https://modelcontextprotocol.io/specification/2024-11-05/basic/transports#stdio.
- MCP tool-schema → OpenAI function-call schema bridge is correct. `must_contain: ["function", "type", "parameters"]` correctly targets `{"type": "function", "function": {"name": ..., "parameters": {...}}}`.
- `subprocess.Popen(stdin=PIPE, stdout=PIPE)` + `communicate()` — appropriate for stdio JSON-RPC. Pedagogically right scope for 50 lines.
- **No invented protocol details.** ✓

**Minor**: tool names referenced (`list_tickets`, `get_ticket_details`) may not match the actual AIE MCP repo's tool names (morning artifact says `list_recent_tickets`, `get_ticket_health`). 30-second cross-check needed against live MCP repo. P2.

### 85165 (M7.S4) — code_review — **B**

Code is 80 lines (course says "60-line diff" — minor narrative drift). Bug analysis:

| Line | Bug | Realistic Kimi mistake? | Marker correct? |
|---|---|---|---|
| **7** | `from pydantic_idempotency import IdempotentRequest` | YES — Kimi K2 frequently invents plausible package names | ✓ |
| **34** | `inventory_client.get(...)` missing `await` | YES — most common Kimi async bug | ✓ |
| **54** | `db.commit()` missing `await` | YES — Kimi pattern-matches sync session.commit() | ✓ |
| **80** | `estimated_delivery="2024-04-30"` hardcoded | Plausible "lazy AI" pattern, less specifically Kimi | ✓ |

All 4 line markers point at the right line. Bug mix is realistic — 3 of 4 are textbook Kimi failure modes.

**Bugs the planted code has but the rubric DOESN'T flag** (P2 polish):
- Line 31 `inventory_client = httpx.AsyncClient()` — never closed/context-managed; production resource leak.
- Lines 65-66 — second `db.commit()` also missing `await`. The `bugs[]` array has a duplicate `line: 54` entry that appears to be a copy-paste error meant for the second commit.
- Lines 33-40 — N consecutive awaited HTTP calls, no `asyncio.gather`. N+1 over network.

**Bug-list duplicate**: `bugs[2]` and `bugs[3]` are byte-identical (`line: 54, "Missing await for async SQLAlchemy commit operation"`). Real bug.

**P0-A2 verdict above**: briefing's generic category callout doesn't NAME `pydantic_idempotency`. Senior eng lands; beginner who sees a fake import burns pip cycles.

---

## Spot-check on 2 non-regenerated steps in modules 5-7 — REGRESSIONS FOUND

### Step 85155 (M4.S2 Implement harness/loop.py) — **P1 REGRESSION**

The morning artifact (and v2) framed the entire course as OpenRouter-based. M0.S2 / M1.S2 / M2.S4 / M3.S2 / M6 all use OpenRouter via OpenAI-compat client + `OPENROUTER_API_KEY`.

But step 85155 (the harness implementation step — single most important code step in M4 since the whole module is "build your own loop") uses:

```python
client = openai.OpenAI(
    base_url="https://api.moonshot.cn/v1",
    api_key=os.getenv("MOONSHOT_API_KEY"),
)
```

`api.moonshot.cn` is Moonshot's direct API (works with `sk-...` Moonshot keys), NOT OpenRouter (works with `sk-or-...` OpenRouter keys). A learner who set up OpenRouter in M0 hits a 401 here. Course-internal coherence regression. One-line fix.

### Step 85164 (M6.S3 Implement POST /orders + tests) — **P1 REGRESSION (capstone)**

The capstone implementation step `demo_data.instructions` tells learners to run:

```bash
$ aider --model openrouter/deepseek/deepseek-chat --map-tokens 2048
```

That is **DeepSeek-Chat, not Kimi K2**. The course title is "Open-Source AI Coding: Aider + **Kimi K2**". A learner who walked the entire course on Kimi K2 hits the capstone — the moment learning is supposed to crystallize — and is told to switch to DeepSeek. Copy-paste leak (probably from a sibling DeepSeek course) that survived because 85164 wasn't on the regen list.

Senior-eng trust-breaker. Fix: change to `openrouter/moonshotai/kimi-k2-0905`.

### Coverage gate copy drift — **P1**

Step 85166 (M6 capstone GHA) briefing reads: *"runs `pytest --cov=app --cov-fail-under=85`"*. Live `lab-grade.yml` on `module-6-capstone` (verified via raw.githubusercontent.com just now) reads `--cov-fail-under=80`. v2 noted the same drift in the morning artifact and called it CLOSED — but the regen didn't actually align them. One-number fix.

---

## Aider command + Moonshot tool_calls + GHA — still clean

Aider commands used in the course: `/architect`, `/code`, `/ask`, `/run`, `/diff`, `/add`, `/commit`, `/undo`, `/test`, `--message-file`, `/read-only` — all real per Aider docs. Aider invocation `aider --model openai/moonshotai/kimi-k2-0905 --openai-api-base https://openrouter.ai/api/v1` — real OpenRouter slug + Aider's OpenAI-compat base flag.

Moonshot tool_calls JSON shape (M4.S1 = 85154): `arguments` is a JSON-encoded string (`"{\"path\": ...}"`), not a dict. Matches OpenAI Chat Completions spec. The 5-step dispatch loop is correctly described.

Capstone GHA: Postgres:16 + ruff blocking + mypy blocking + pytest with `--cov-fail-under=80` + htmlcov upload on failure. Real capstone gate. Coverage gate is **80** (the drift is in 85166's narrative, not the gate itself).

---

## Verdict

**SHIP-WITH-FIXES** — not v2's SHIP-AS-CANONICAL.

| Item | Status |
|---|---|
| P0-A1 (M5 cross-course MCP gloss) | **CLOSED** |
| P0-A2 (M7.S4 planted-hallucination flag) | **STILL OPEN soft form** |
| Beginner-found M3.S3 fabricated Aider feature | **CLOSED** |
| Aider command accuracy | CLEAN |
| Moonshot tool_calls JSON shape | CORRECT |
| Capstone GHA `lab-grade.yml` | CORRECT (80% gate) |
| **R1**: 85155 harness uses Moonshot-direct, not OpenRouter | **REGRESSION** |
| **R2**: 85164 capstone uses `deepseek-chat`, not Kimi | **REGRESSION** |
| **R3**: 85166 narrative says `--cov-fail-under=85`, actual is 80 | **DRIFT** |

### Concrete remaining blockers (~20 min total)

1. **85165**: add ONE sentence — *"One planted bug is `pydantic_idempotency` — fictional on purpose; do not pip-install. Demonstrates a real Kimi K2 hallucination class."* Closes P0-A2 fully.
2. **85165**: fix duplicate `bugs[]` entry on line 54; the second commit is on lines 65-66.
3. **85155**: change `base_url="https://api.moonshot.cn/v1"` → `https://openrouter.ai/api/v1`; `MOONSHOT_API_KEY` → `OPENROUTER_API_KEY`.
4. **85164**: change `aider --model openrouter/deepseek/deepseek-chat` → `aider --model openrouter/moonshotai/kimi-k2-0905`.
5. **85166**: change narrative `--cov-fail-under=85` → `--cov-fail-under=80`.

### Optional polish (~10 min)

6. **85152**: replace `select_related/prefetch_related` (Django) with `selectinload/joinedload` (SQLAlchemy).
7. **85138**: promote "no vendor lock-in" to verbatim "Zero Anthropic dependency" in the IS card.
8. **85165**: add 1-2 more bugs (httpx.AsyncClient context-management, missing asyncio.gather). 4 unique bugs is thin for a "60-line" review.

---

## Would I sign off shipping this for a peer joining my team?

**Conditional yes.** Without items 1-5, no — a senior eng on the capstone hits the `deepseek-chat` line and instantly mistrusts the rest. With items 1-5 fixed (∼20 min of narrow per-step JSON edits, no whole-course regen needed per CLAUDE.md §"Always Regen EXACTLY what is broken"), this is a B+ course on a real-team-onboarding bar:

- Pedagogy is strong (feel-the-pain → AGENTS.md → measure-delta → own-the-loop → MCP-adapter → GHA capstone). Same inversion AIE validated.
- Aider command surface is honest. No fabricated slash commands; real flags; real config.
- Moonshot tool_calls shape teaches the JSON-encoded-arguments detail that LLM tutorials routinely paraphrase wrong. Highest-leverage thing taught.
- MCP-from-non-Anthropic-LLM is the piece every Anthropic-tilted MCP tutorial skips. Worth the course alone.
- GHA capstone is a real gate (Postgres + ruff + mypy + 80% cov + Testcontainers). Not a toy.

This is a multi-step regen leaving downstream non-regenerated steps misaligned. Right move per CLAUDE.md: per-step regen on 85155 + 85164 + 85165 with explicit feedback bodies pointing at the leaks. 5-10 min compute; no whole-course regen; no risk of regressing the 25 currently-clean steps. Then ship.

— reviewed, 2026-04-25 (round-5 v3)
