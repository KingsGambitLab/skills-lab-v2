fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
aiosqlite==0.20.0
# 2026-04-25 v8 (deploy/postgres-multi-user) — Postgres async driver. Used when
# DATABASE_URL is set to `postgresql+asyncpg://...` (production multi-user
# instance on 52.88.255.208). Local dev keeps using aiosqlite + skills_lab.db
# unless DATABASE_URL is overridden. backend/database.py auto-detects which
# branch to take based on the URL scheme.
asyncpg==0.29.0
pydantic==2.9.2
httpx==0.27.2
python-multipart==0.0.12
# D.2 (2026-04-21) language-aware sandbox: PyYAML for YAML parsing, jsonschema
# for optional YAML/JSON schema validation. sqlite3 is stdlib so no addition needed.
pyyaml>=6.0
jsonschema>=4.22
# Headless browser for post-generation automated QC (backend/auto_review.py).
# After install, also run: .venv/bin/playwright install chromium
playwright==1.47.0
celery==5.6.3
