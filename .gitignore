__pycache__/
*.pyc
*.pyo
*.db
*.db-shm
*.db-wal
*.sqlite
.env
venv/
.venv/
*.egg-info/
dist/
build/
.DS_Store

# Runtime state — do NOT commit
.anthropic_budget.json

# Macro / editor noise
.idea/
.vscode/
*.swp

# Solver-agent sandbox artifacts (do NOT commit)
cloudsync-minimal/
web-api/
fake-PYREPL_TRACE-value

# Transient logs from review pipeline
reviews/*.log

# Redis dump files (runtime artifact)
dump.rdb

# Celery control-exchange files (runtime artifact)
control/
